{
  "name": "@etco-job-application/core",
  "version": "0.0.0",
  "private": true,
  "license": "NOLICENSE",
  "main": "src/index.ts",
  "scripts": {
    "build": "exit 0",
    "checks": "yarn typecheck && yarn lint",
    "typecheck": "../../node_modules/.bin/tsc -p . --noEmit",
    "lint": "../../node_modules/.bin/eslint '**/*.ts{,x}'",
    "lint:fix": "../../node_modules/.bin/eslint --fix '**/*.ts{,x}'"
  },
  "dependencies": {
  },
  "devDependencies": {
  }
}
